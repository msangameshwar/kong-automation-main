_format_version: "3.0"
services:
  - name: mock-service
    url: http://mockbin.org
    routes:
      - name: mock-route
        paths:
          - /mock
        methods:
          - GET
plugins:
  - name: rate-limiting
    service: mock-service
    config:
      minute: 5
      policy: local
